# Prettier ignore file for velocity.report

# Agent/worktree metadata
.claude/

# Build artifacts
.build/
tools/visualiser-macos/build/
tools/visualiser-macos/DerivedData/
build/
dist/

# Vendored third-party sources — not ours to reformat. The format-docs target
# globs '**/*.md' from the repo root, so without this every docs commit rewrites
# libpcap's own README and INSTALL files as incidental churn.
third_party/

# Dependencies
node_modules/
.venv/
venv/
.tox/

# Test / tool caches
.pytest_cache/

# Third-party submodule content
image/.pi-gen/

# Generated files
proto/
*.pb.go
*.pb.swift
*_pb2.py

# Coverage reports
coverage/
htmlcov/
*.lcov

# Package manager files
package-lock.json
pnpm-lock.yaml
yarn.lock

# Eleventy templates live under public_html/, but format-docs only targets
# Markdown. Keep the Markdown guides included in repo-wide docs formatting.

# Web tree (owned by web/.prettierrc — run format-web separately)
web/

# macOS
.DS_Store
