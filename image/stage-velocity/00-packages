# Core runtime
python3
python3-pip
python3-venv

# LaTeX — full TeX Live for PDF generation (Phase 1: no size reduction)
texlive-xetex
texlive-fonts-recommended
texlive-latex-extra
texlive-fonts-extra
latexmk

# LiDAR support (included but disabled by default at runtime)
libpcap-dev

# RS-232 HAT support
raspi-config
python3-serial
minicom

# System utilities
sqlite3
jq
curl
