#!/usr/bin/env bash
# velocity-update — REMOVED
#
# This wrapper script has been replaced by velocity-ctl, a purpose-built
# Go binary for on-device management. Run:
#
#   sudo velocity-ctl upgrade              # check + download + apply latest
#   sudo velocity-ctl upgrade --check      # check for newer version only
#   sudo velocity-ctl upgrade --binary /p  # apply a local binary (offline)
#   sudo velocity-ctl rollback             # restore previous version
#   sudo velocity-ctl backup               # snapshot binary + database
#
# See: docs/plans/deploy-rpi-imager-fork-plan.md § 8

echo "velocity-update has been replaced by velocity-ctl." >&2
echo "Run: sudo velocity-ctl upgrade" >&2
exit 1
