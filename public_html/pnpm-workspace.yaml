# pnpm 11 no longer reads the "pnpm" field from package.json, and it replaced the
# `onlyBuiltDependencies` list with the `allowBuilds` map.
allowBuilds:
  '@parcel/watcher': true

overrides:
  liquidjs@<=10.25.4: 10.25.5
  minimatch@<3.1.5: 3.1.5
  picomatch@<2.3.2: 2.3.2
